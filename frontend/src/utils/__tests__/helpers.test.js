const { utilityFunction } = require('../index'); // Adjust the import based on your utility function's location

test('utilityFunction should return expected result', () => {
	expect(utilityFunction()).toBe('expected result'); 
});